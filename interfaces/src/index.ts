const app = document.getElementById("app");
if (app) {
  app.innerHTML = `
  <h1>Interfaces</h1>
  <p>The code for this practice is in <code>./src/interfaces.ts</code>. Complete the instructions in that file, and make sure your types are all passing.
  `;
}
